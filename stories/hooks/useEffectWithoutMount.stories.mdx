import { useEffectWithoutMount } from '../../src'

<Meta title="Hooks/useEffectWithoutMount" />

# useEffectWithoutMount

React effect hook that ignores the first invocation (e.g. on mount). The signature is
exactly the same as the useEffect hook

## Usage

```js
import { useEffectWithoutMount } from '@drewster/neutron-react'
```
