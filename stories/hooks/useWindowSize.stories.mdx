import { useWindowSize } from '../../src'

<Meta title="Hooks/useWindowSize" />

# useWindowSize

Custom hook that returns bindings for focus event.

## Usage

```js
import { useWindowSize } from '@drewster/neutron-react'
```
